import SourceIntakePage from '@/components/source-intake-page';
import { PATHWAYS_BRAND } from '@/lib/brand';

export default function Page() {
  return <SourceIntakePage brand={PATHWAYS_BRAND} />;
}
