import SourceReviewPage from '@/components/source-review-page';
import { PATHWAYS_BRAND } from '@/lib/brand';

export default function Page() {
  return <SourceReviewPage brand={PATHWAYS_BRAND} />;
}
