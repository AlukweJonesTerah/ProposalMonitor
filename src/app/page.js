import DashboardPage from '@/components/dashboard-page';
import { PROPOSAL_MONITOR_BRAND } from '@/lib/brand';

export default function Page() {
  return <DashboardPage brand={PROPOSAL_MONITOR_BRAND} />;
}
