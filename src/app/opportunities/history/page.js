import HistoryPage from '@/components/history-page';
import { PROPOSAL_MONITOR_BRAND } from '@/lib/brand';

export default function Page() {
  return <HistoryPage brand={PROPOSAL_MONITOR_BRAND} />;
}
