import SourceReviewPage from '@/components/source-review-page';
import { PROPOSAL_MONITOR_BRAND } from '@/lib/brand';

export default function Page() {
  return <SourceReviewPage brand={PROPOSAL_MONITOR_BRAND} />;
}
