import SourceIntakePage from '@/components/source-intake-page';
import { PROPOSAL_MONITOR_BRAND } from '@/lib/brand';

export default function Page() {
  return <SourceIntakePage brand={PROPOSAL_MONITOR_BRAND} />;
}
