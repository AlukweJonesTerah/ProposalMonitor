'use client';
import Link from 'next/link'; import { useState } from 'react'; import AppHeader from '../../app-header';
export default function SourceIntake() { const [urls,setUrls]=useState(''); const [notice,setNotice]=useState(''); const submit=async(e)=>{e.preventDefault();const r=await fetch('/api/sources',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({urls})});const d=await r.json();setNotice(r.ok?`${d.added} source(s) submitted for review.`:d.error);if(r.ok)setUrls('');};return <><AppHeader /><main className="historyPage"><Link className="backLink" href="/">← Back to dashboard</Link><h1>Source intake</h1><p>Submit public tender, donor, or ministry portals for review.</p><section className="historyEmpty"><form className="sourceAdd intakePage" onSubmit={submit}><input value={urls} onChange={e=>setUrls(e.target.value)} placeholder="https://public-portal.example/opportunities" required/><button>Submit source</button></form>{notice&&<p>{notice}</p>}<Link href="/sources/review">Review submitted sources →</Link></section></main></>; }
