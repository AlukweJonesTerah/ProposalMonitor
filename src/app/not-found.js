import NotFoundPage from '@/components/not-found-page';
import { PROPOSAL_MONITOR_BRAND } from '@/lib/brand';

export default function NotFound() {
  return <NotFoundPage brand={PROPOSAL_MONITOR_BRAND} />;
}
